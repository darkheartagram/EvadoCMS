<%= form_for(@category_cv) do |f| %>
  <% if @category_cv.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category_cv.errors.count, "error") %> prohibited this category_cv from being saved:</h2>

      <ul>
      <% @category_cv.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
