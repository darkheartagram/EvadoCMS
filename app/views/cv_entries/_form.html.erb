<%= form_for([@cv, @category_cv, @cv_entry]) do |f| %>
  <% if @cv_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cv_entry.errors.count, "error") %> prohibited this cv_entry from being saved:</h2>

      <ul>
      <% @cv_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :entry %><br />
    <%= f.text_field :entry %>
  </div>
  <div class="field">
    <%= f.label :class_name %><br />
    <%= f.text_field :class_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
